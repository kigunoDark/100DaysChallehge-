<%- include('../layouts/navbar.ejs')%>
    <div  id="resetFrom">
        <form action="/admin/new-password", method="POST">
            <div class="container resetFrom">
                <p>
                    <div class =" row">
                        <div class="col s12 m12">
                            <h5 class="center mobileTitle" > Сменить пароль </h5>
                                

                                <div class="input-field col s12">
                                    <i class="material-icons prefix">lock</i>
                                    <input id="icon_password" type="password" class="validate " name="adNewPassword">
                                    <label for="icon_password">Новый пароль</label>
                                </div>
                                <input type="hidden" name="adminId" value="<%=adminId%>">
                                <input type="hidden" name="passwordToken" value="<%=  passwordToken %>">
                                <input type="hidden" name="_csrf" value="<%=csrfToken%>">
                                <button class="right loginButton"  type="submit">Изменить пароль</button>
                        </div>
                    </div>
                </p>
            </div>
        </form>
    </div>
<%- include('../layouts/footer.ejs') %>